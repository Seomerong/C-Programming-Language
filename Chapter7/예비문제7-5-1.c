#include<stdio.h>
int main(void)
{
	char a[10];
	scanf("%s", &a);
	char b[] = "code";
	if (a[0]==b[0]&& a[0] == b[0] && a[0] == b[0] && a[0] == b[0]) {
		printf("프로그램을 종료합니다.\n");
		}
	else {
		printf("비밀번호를 다시 입력하세요.\n");
		}

	return 0;
}